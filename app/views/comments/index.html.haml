%h1 Listing comments

%table
  %thead
    %tr
      %th
      %th
      %th
      %th
  %tbody
    - @comments['models'].each do |comment|
      %tr
        %td= comment.body
        %td= link_to 'Show', comment
        %td= link_to 'Edit', edit_comment_path(comment)
        %td= link_to 'Destroy', comment, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Comments', new_comment_path
